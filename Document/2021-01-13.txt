+++++++++++++++++++++++++      2021-01-13    +++++++++++++++++++++++++++

---> Today i have learn from videos
    - DOM(document object model)
        - stuctured representation of an HTML document
    - what are events
        - events
            - clicking
            - scrolling
            - pressing a key
            - resizing a window
        - events listener
    - how to access the element from html page
    - how to write text content 
    - understand the game logic
    - How to set up an event handler
    - what an anonymous function is
    - How to change the image in an <img> element

--> solve the challenge3 of tip calculater

--> solve the challenge4 game using the object and function in side the object  

--> then ptactice on HTML and CSS
    - tag in HTML5
        - audio
        - canvas
        - footer
        - header
        - main
        - nav
        - picture
        - time
        - track

    - solve the challenge given by ma'am using the following code
    - function lsTest() {
            try {
                localStorage.setItem('name', 'mehul');
                localStorage.removeItem('name');
                return true;
            } catch (e) {
                return false;
            }
        }
    
    - if (lsTest() === true) {
        alert('LocalStorage is Enabled');
    } else {
        alert('LocalStorage is disabled');
    }